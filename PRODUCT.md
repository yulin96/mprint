# mprint

mprint 是面向内部业务系统的 Windows 本地打印工具。它作为登录用户下的托盘常驻程序运行，通过 `127.0.0.1` HTTP API 向普通网页暴露打印能力。

首版用户是部署和调试打印业务的开发、实施人员。核心任务是确认服务运行、查看打印机，并在系统默认浏览器打开本地可视化编辑器，配置纸张和打印元素、预览版式并执行测试打印。

产品优先保证调用简单、状态明确、参数可测试。当前支持文字、已安装系统字体、通过打印请求声明的 HTTPS 远程字体、Data URL 图片、毫米定位、自定义纸张、指定打印机和静默打印。模板编辑器可读取浏览器授权的系统字体，也允许直接输入字体名称；远程字体由 `fonts` 数组声明，不能把 URL 当作字体名称。macOS 只作为开发验证环境，正式目标是 Windows。
